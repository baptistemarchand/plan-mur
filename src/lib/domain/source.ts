/**
 * Dépôt d'où sort le code qui tourne, affiché en pied de page.
 *
 * L'AGPL demande que les personnes qui utilisent le service puissent en obtenir
 * la source : un fork déployé tel quel renverrait les siennes ici, vers un dépôt
 * qui n'est pas le sien. Cette URL est donc à changer en même temps que le
 * `name` et le `database_id` de wrangler.jsonc.
 */
export const SOURCE_URL = 'https://github.com/baptistemarchand/plan-mur'
